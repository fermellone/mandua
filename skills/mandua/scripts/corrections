#!/bin/sh
set -eu
script_dir=$(CDPATH='' cd -- "$(dirname -- "$0")" && pwd)
project_dir=$(CDPATH='' cd -- "$script_dir/../../.." && pwd)
exec uv run --no-editable --locked --project "$project_dir" python "$script_dir/corrections.py" "$@"
